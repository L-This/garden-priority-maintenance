# نظام مؤشر أولوية صيانة الحدائق - نسخة Supabase

الصفحات:
- `/dashboard` لوحة النتائج
- `/assessment` إدخال تقييم جديد

## خطوات التشغيل على Vercel

### 1) شغل SQL في Supabase
افتح:
Supabase > SQL Editor > New Query

انسخ محتوى الملف:
`supabase/schema.sql`

ثم اضغط Run.

### 2) أضف متغيرات البيئة في Vercel
Project Settings > Environment Variables

أضف:

```text
NEXT_PUBLIC_SUPABASE_URL=https://ydyrpkstkuhbaqwhlwdp.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ضع_anon_key_هنا
```

ثم Redeploy.

### 3) التجربة
افتح `/assessment` وأدخل تقييم جديد مع صور.
ثم افتح `/dashboard` واضغط تحديث البيانات.

## ملاحظة أمنية
السياسات الحالية مفتوحة للتجربة فقط. في النسخة الإنتاجية نضيف تسجيل دخول وصلاحيات للمشرفين.


## تحديث الأزرار

تم تعديل لوحة النتائج لتدعم:
- عرض البيانات الحقيقية من Supabase
- عرض البيانات التجريبية من الكود فقط بدون حفظها في القاعدة
- حذف البيانات التجريبية القديمة من القاعدة
- عدم خلط التجريبي مع الحقيقي

مهم: شغّل `supabase/schema.sql` مرة أخرى لإضافة عمود `is_demo`.


## المرحلة الثانية
تمت إضافة:
- بحث باسم الحديقة أو المشروع أو الحي
- فلترة حسب المشروع
- فلترة حسب الأولوية
- تصدير CSV للنتائج الظاهرة
- حذف تقييم حقيقي من Supabase من داخل لوحة النتائج

مهم: شغّل ملف `supabase/schema.sql` مرة أخرى لإضافة سياسات الحذف.


## مرحلة بوابات المشاريع
تم تعديل لوحة النتائج لتبدأ ببوابات:
- كافة المشاريع
- بوابة لكل مشروع

لا تظهر المؤشرات إلا بعد اختيار بوابة، ثم تظهر بيانات الحدائق بعد الضغط على أحد المؤشرات.
تم حذف جدول البحث وزر حذف البيانات التجريبية من القاعدة.


## V3 - قاعدة الهيكل الإداري

تمت إضافة:
- جدول الموظفين employees
- جدول المشاريع projects
- جدول الحدائق gardens
- ربط التقييمات بالموظف والمشروع والحديقة
- صفحة `/admin-data` لإدارة الموظفين والمشاريع والحدائق
- صفحة `/assessment` صارت تعتمد على القوائم من Supabase بدل الكتابة اليدوية

بعد رفع النسخة:
1. شغّل `supabase/schema.sql`
2. افتح `/admin-data`
3. أضف الموظفين ثم المشاريع ثم الحدائق
4. قيّم من `/assessment`
